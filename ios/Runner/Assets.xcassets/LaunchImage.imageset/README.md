# Launch Screen Assets

Figma full-bleed splash (iPhone portrait, iPad landscape, light and dark).

Regenerate from Figma, then paint over the fake status bar and home indicator
so iOS can draw the real chrome. Do not run `flutter_native_splash:create`
with `ios: true`, that overwrites these files.
